# Dependances de SpiceUtils. Versions epinglees = combo valide de bout en bout.

# --- Interface (WebView + tray) ---
pywebview==6.2.1
pythonnet==3.1.0
pystray==0.19.5
Pillow==11.3.0

# --- Serveur HTTP ---
flask==3.1.3
flask-cors==6.0.5

# --- Recuperation de l'audio source (necessite ffmpeg, installe par winget) ---
yt-dlp==2026.6.9

# --- Separation des stems ---
demucs==4.0.1

# torch/torchaudio epingles en 2.2.2 : sauvegarde du WAV via soundfile, SANS
# torchcodec ni DLL partagees de FFmpeg (torchaudio >= 2.8 casse l'ecriture).
torch==2.2.2
torchaudio==2.2.2
soundfile==0.14.0

# torch 2.2.2 est compile contre l'ABI NumPy 1.x : NumPy 2.x casse.
numpy==1.26.4
